---
import Main from '../layouts/Main.astro';
import ExampleImage from '../assets/images/example.png';
import Image from 'astro/components/Image.astro';
import type { User } from '../lib/models';

const userData: User = Astro.locals.user;
---

<Main title="Home">
	<main>
		<div class="interaction-container">
			<div class="text-section">
				<h1>Welcome { userData?.name || "Player" }</h1>
				<p>
					In wordle CHAMPIONS, you can play as many Wordle games in a row as you want.
          The only question is: How far can you make it? <br> Instead of one word per day,
          you will get a new word each time you guessed one correctly. Based on how close your
          guesses are to the solution, you will receive points. You will also receive points for how
          fast you guess correctly and how long you endure. <br> After six unsuccessful guesses, it's
          over. What will your highscore be?
				</p>
			</div>
			<Image src={ ExampleImage } class:list={'example-image'} alt="An example picture of the wordle CHAMPIONS Classic Mode gameplay.">
		</div>
	</main>
</Main>

<style>
	main {
		padding: 2rem;
		width: 100%;
	}

	p {
		line-height: 1.75em;
		text-align: left;
	}

  br {
    height: .2rem;
  }

	.interaction-container {
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		display: flex;
		flex-direction: column;
		gap: 2rem;
    padding-left: 1rem;
		border-left: 3px solid var(--border);
    width: 48rem;
	}

	.text-section {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: .5rem;
	}

  .example-image {
    aspect-ratio: 1171 / 716;
    width: 80%;
    height: auto;
    box-shadow: 0 2px 4px 2px rgba(0, 0, 0, 0.5);
    margin: 0 auto;
  }
</style>